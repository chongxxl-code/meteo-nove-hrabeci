name: Collect CHMI regional rain

# CHMI OpenData "now" files contain 10-minute observations and are refreshed hourly.
# Re-trigger after fixing the official 10m data path.
on:
  schedule:
    - cron: '7 * * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/collect_chmi.py'
      - '.github/workflows/collect-chmi.yml'

permissions:
  contents: write

concurrency:
  group: chmi-rain-collector
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Collect nearest live CHMI 10-minute rain data
        run: python scripts/collect_chmi.py
      - name: Commit rain observations
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/chmi-status.json data/observations/ 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No observation changes."
            exit 0
          fi
          git commit -m "data: CHMI rain $(date -u +'%Y-%m-%d %H:%M UTC')"
          git pull --rebase origin main
          git push
