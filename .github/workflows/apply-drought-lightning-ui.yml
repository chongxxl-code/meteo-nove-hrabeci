name: Apply drought and CHMI lightning UI

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/apply_drought_lightning_ui.py'
      - '.github/workflows/apply-drought-lightning-ui.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/apply_drought_lightning_ui.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add alerts.html about.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "site: show drought context and CHMI lightning"
          git pull --rebase origin main
          git push
